#include "main.h"

/**
 * _puts - Prints a string
 * @s: the string to be read
 *
 * Return: success 0
 */
void _puts(char *str)
{
	int i;

	for (i = 0; *(s + i) != '\0'; i++)
	{
		_putchar(*(s + i));
	}

	return (0);
}
